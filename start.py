import os

# A script to start up the local development environment with an 
# instance of celery worker, celery beat, django server and 
# rabbitmq server running on different processes

# run the start up script
os.system("bash ./startup/runserver.sh")



